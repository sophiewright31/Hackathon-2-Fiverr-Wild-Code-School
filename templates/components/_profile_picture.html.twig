<div class="dropdown-menu border-0 ml-5" aria-labelledby="dropdownMenuButton">
    <div class="row mt-5 ml-5" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="frame text-white bg-primary mb-3" style="height:500px; border-radius:45px;">
            <div class="card-body">
                {% if app.user %}
                    <div class="row justify-content-center">
                        <div class="d-flex ">
                            <img class="avatar mt-3" src="{{ app.user.imagePicture }}"
                                 alt="avatar">
                        </div>
                    </div>
                {% endif %}
                <div class="row justify-content-center">
                    <a href="{{ path('content_index') }}" class="btn btn-success btn-lg m-3" role="button"
                       aria-pressed="true">SELECT</a><br>
                </div>
                <div class="row justify-content-center">
                    <a href="{{ path('content_new') }}" class="btn btn-success btn-lg m-3" role="button"
                       aria-pressed="true">CREATE</a><br>
                </div>
                <div class="row justify-content-center m-3">
                    <a href="{{ path('content_index') }}" class="btn btn-success btn-lg" role="button"
                       aria-pressed="true">MINES</a><br>
                </div>
            </div>
        </div>
    </div>
</div>